public final class Parent {

    // if we are using final keyword with parent class then we can not extend that class
    // or we can not create the child class of parent class
    public final void hello(){
        // we can use final keyword with methods
        // This method can never be override
    }
}
